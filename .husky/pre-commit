#!/bin/sh
# 阻擋在根目錄新增 .py, .js, .mjs, .cjs (白名單如 config 檔除外)
if git diff --cached --name-only --diff-filter=A | grep -E '^[^/]+\.(py|js|mjs|cjs)$'; then
  echo "CRITICAL: Scripts are not allowed in the root directory!"
  echo "Please move them to scripts/pipeline/ and use sub-commands."
  exit 1
fi
